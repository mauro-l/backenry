/* const baseUrl = import.meta.env.VITE_BACKEND_URL;
console.log("env", baseUrl);
const productsUrl = baseUrl + "/api/products";
const cartsUrl = baseUrl + "/api/carts";

export { baseUrl, productsUrl, cartsUrl };
 */
